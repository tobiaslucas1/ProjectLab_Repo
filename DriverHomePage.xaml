<Page x:Class="Test.DriverHomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DriverHomePage">

    <ScrollViewer>
        <Grid Background="White">
            
            <Border Background="#4C1130" Height="35" VerticalAlignment="Top">
                <Grid>
                    <Image Source="/Drive2Gether_logo.jpg" Height="28" Width="28" VerticalAlignment="Center" Margin="10,0,0,0" HorizontalAlignment="Left"/>
                    <TextBlock VerticalAlignment="Center" FontWeight="Bold" Margin="50,0,0,0" Foreground="White">
                        <Hyperlink Click="Home_Click" Foreground="White" TextDecorations="{x:Null}">Drive2Gether</Hyperlink>
                    </TextBlock>
                    <Button Content="settings" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="14" HorizontalAlignment="Right" Margin="0,0,10,0" Click="Settings_Click"/>
                </Grid>
            </Border>

            
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,60,0,0">
                <Button Content="Create a Roadtrip" Width="150" Height="40" Click="CreateRoadTrip_Click" Margin="0,0,0,10"/>
                <Button Content="Bekijk chats" Width="150" Height="40" Click="OpenChat_Click"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
